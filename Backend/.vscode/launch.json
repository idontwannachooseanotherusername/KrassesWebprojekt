{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Start",
            "request": "launch",
            "command": "nvm use v12.10; node server.js",
            "cwd": "${workspaceFolder}"
        },
        {
            "type": "node-terminal",
            "name": "Debug",
            "request": "launch",
            "command": "nvm use v12.10; node server.js --inspect",
            "cwd": "${workspaceFolder}"
        }
    ]
}